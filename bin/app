#!/bin/bash
yarn client-build --mode staging --base=/triple_tee_app/
app_dir="../triple_tee_app"
pushd $app_dir
  git checkout gh-pages
  rm -r assets
  cp -r ../triple_tee-source/dist/public/* ${app_dir}/
popd
